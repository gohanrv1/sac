╔════════════════════════════════════════════════════════════════╗
║                 🚀 GUÍA RÁPIDA DE DESPLIEGUE                  ║
║                    Aplicación Taxi - Docker                    ║
╚════════════════════════════════════════════════════════════════╝

┌────────────────────────────────────────────────────────────────┐
│ 📋 OPCIÓN 1: Despliegue Rápido (Recomendado)                 │
└────────────────────────────────────────────────────────────────┘

Windows:
  1. Abre PowerShell o CMD en la carpeta del proyecto
  2. Ejecuta: start.bat
  3. Espera 30 segundos
  4. Abre: http://localhost:8080

Linux/Mac:
  1. chmod +x start.sh
  2. ./start.sh
  3. Abre: http://localhost:8080

┌────────────────────────────────────────────────────────────────┐
│ 🛠️ OPCIÓN 2: Despliegue Manual                               │
└────────────────────────────────────────────────────────────────┘

1. docker-compose up -d --build
2. Espera que los servicios inicien
3. Accede a http://localhost:8080

┌────────────────────────────────────────────────────────────────┐
│ 🌍 DESPLIEGUE EN SERVIDOR (Producción)                       │
└────────────────────────────────────────────────────────────────┘

1. Sube el proyecto al servidor:
   scp -r . usuario@servidor:/var/www/taxi

2. En el servidor:
   cd /var/www/taxi
   
3. IMPORTANTE - Cambia las contraseñas en docker-compose.prod.yml:
   - MYSQL_ROOT_PASSWORD
   - MYSQL_PASSWORD
   - DB_PASS

4. Ejecuta:
   docker-compose -f docker-compose.prod.yml up -d --build

5. Configura firewall (si es necesario):
   sudo ufw allow 80/tcp
   sudo ufw allow 443/tcp

┌────────────────────────────────────────────────────────────────┐
│ 🔗 URLs DE ACCESO                                             │
└────────────────────────────────────────────────────────────────┘

Aplicación Web:    http://localhost:8080
PHPMyAdmin:        http://localhost:8081

Producción:        http://tu-dominio.com

┌────────────────────────────────────────────────────────────────┐
│ 🔐 CREDENCIALES POR DEFECTO                                   │
└────────────────────────────────────────────────────────────────┘

MySQL:
  Usuario:   root
  Password:  taxi_password_2026
  
⚠️  CAMBIA ESTAS CREDENCIALES EN PRODUCCIÓN

┌────────────────────────────────────────────────────────────────┐
│ 📊 COMANDOS ÚTILES                                            │
└────────────────────────────────────────────────────────────────┘

Ver estado:           docker-compose ps
Ver logs:             docker-compose logs -f web
Reiniciar:            docker-compose restart
Detener:              docker-compose down
Backup BD:            ./backup.sh  (o backup.bat en Windows)

┌────────────────────────────────────────────────────────────────┐
│ 🔄 ACTUALIZAR APLICACIÓN                                      │
└────────────────────────────────────────────────────────────────┘

Después de hacer cambios en el código:

docker-compose restart web

Si cambias el Dockerfile:

docker-compose up -d --build web

┌────────────────────────────────────────────────────────────────┐
│ 💾 BACKUPS                                                    │
└────────────────────────────────────────────────────────────────┘

Crear backup manual:
  Windows: backup.bat
  Linux:   ./backup.sh

Restaurar backup:
  docker exec -i taxi_db mysql -u root -ptaxi_password_2026 infotaxi < backup.sql

Los backups se guardan en: ./backups/

┌────────────────────────────────────────────────────────────────┐
│ 🐛 SOLUCIÓN DE PROBLEMAS                                      │
└────────────────────────────────────────────────────────────────┘

Puerto ocupado:
  Cambia el puerto en docker-compose.yml:
  ports: - "8082:80"  # Cambiar 8080 por otro

Error de permisos:
  docker exec taxi_app chown -R www-data:www-data /var/www/html

Base de datos no conecta:
  docker-compose restart db
  docker-compose logs db

Contenedores no inician:
  docker-compose down -v
  docker-compose up -d --build

┌────────────────────────────────────────────────────────────────┐
│ 📂 ARCHIVOS DOCKER CREADOS                                    │
└────────────────────────────────────────────────────────────────┘

✅ Dockerfile                  - Imagen de la aplicación
✅ docker-compose.yml          - Desarrollo/Testing
✅ docker-compose.prod.yml     - Producción
✅ .dockerignore               - Archivos excluidos
✅ .htaccess                   - Configuración Apache
✅ start.sh / start.bat        - Scripts de inicio rápido
✅ backup.sh / backup.bat      - Scripts de backup
✅ README_DOCKER.md            - Documentación completa

┌────────────────────────────────────────────────────────────────┐
│ ⚡ CARACTERÍSTICAS                                            │
└────────────────────────────────────────────────────────────────┘

✅ PHP 8.1 con Apache
✅ MySQL 8.0
✅ PHPMyAdmin incluido
✅ Auto-importación de base de datos
✅ Permisos configurados automáticamente
✅ Volúmenes persistentes
✅ Health checks
✅ Restart automático
✅ Optimizado para producción

┌────────────────────────────────────────────────────────────────┐
│ 📞 SOPORTE                                                    │
└────────────────────────────────────────────────────────────────┘

Documentación completa: README_DOCKER.md
Logs de la app:         docker-compose logs web
Logs de MySQL:          docker-compose logs db

╔════════════════════════════════════════════════════════════════╗
║           ¡Listo para desplegar en cualquier servidor!        ║
╚════════════════════════════════════════════════════════════════╝
